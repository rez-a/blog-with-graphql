import { Typography } from '@mui/material';
import React from 'react';

const Footer = () => {
    return (
        <Typography component="p" variant='p' color="primary" bgcolor="#f7f7f7" textAlign="center" p={1}>پروژه وبلاگ با GraphQL</Typography>
    );
};

export default Footer;